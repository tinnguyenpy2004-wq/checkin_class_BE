@model AttendanceSystemProject.ViewModels.TeacherViewModel

<div class="space-y-8">

    <!-- Lớp học quản lý -->
    <div>
        <h2 class="text-2xl font-semibold mb-3">Các lớp học bạn đang quản lý</h2>
        <div class="bg-white shadow-md rounded-lg overflow-hidden">
            @if (Model.ManagedClasses.Any())
            {
                <table class="min-w-full leading-normal">
                    <thead>
                        <tr class="border-b-2 border-gray-200 bg-gray-50 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">
                            <th class="px-5 py-3">ID Lớp</th>
                            <th class="px-5 py-3">Tên Lớp</th>
                            <th class="px-5 py-3">Mã Lớp</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var cls in Model.ManagedClasses)
                        {
                            <tr class="border-b border-gray-200 hover:bg-gray-50">
                                <td class="px-5 py-4 text-sm">@cls.ClassId</td>
                                <td class="px-5 py-4 text-sm">@cls.ClassName</td>
                                <td class="px-5 py-4 text-sm">@cls.Code</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="p-5 text-gray-500">Bạn chưa quản lý lớp học nào.</p>
            }
        </div>
    </div>

    <!-- Sự kiện tổ chức -->
    <div>
        <h2 class="text-2xl font-semibold mb-3">Các sự kiện bạn tổ chức</h2>
        <div class="bg-white shadow-md rounded-lg overflow-hidden">
            @if (Model.ManagedEvents.Any())
            {
                <table class="min-w-full leading-normal">
                    <thead>
                        <tr class="border-b-2 border-gray-200 bg-gray-50 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">
                            <th class="px-5 py-3">ID Sự kiện</th>
                            <th class="px-5 py-3">Tên Sự kiện</th>
                            <th class="px-5 py-3">Mã Sự kiện</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var evt in Model.ManagedEvents)
                        {
                            <tr class="border-b border-gray-200 hover:bg-gray-50">
                                <td class="px-5 py-4 text-sm">@evt.EventId</td>
                                <td class="px-5 py-4 text-sm">@evt.EvenName</td>
                                <td class="px-5 py-4 text-sm">@evt.Code</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="p-5 text-gray-500">Bạn chưa tổ chức sự kiện nào.</p>
            }
        </div>
    </div>
</div>